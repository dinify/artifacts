const { toPairs } = require("ramda");

const robotoXHeight = 0.5283203125;
const root = 16;

const typeScale = {};

toPairs();
const fontSizePx = relativeXHeight * size;
const fontSize = fontSizePx / root;
const letterSpacing = fontSizePx / tracking;
